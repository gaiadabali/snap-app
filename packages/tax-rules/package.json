{
  "name": "@snap/tax-rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pluggable per-country tax engine. A pack is DATA (rates, allowances, document rules); the interpreter is fixed code that reads it. One pack active at a time, swappable by country without shipping a build.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "sideEffects": false,
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
